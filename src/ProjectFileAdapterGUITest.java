import javafx.application.Application;

// If you want to run this class, remember to change the file path in the file
// variable in the class ProjectFileAdapterGUI
// Also if you get an IO Error running this file and the are no projects
// displayed in the GUI just run the ColourIT class to load some initial date.
// Remember to change the file path there too.

public class ProjectFileAdapterGUITest
{
  public static void main(String[] args)
  {
    Application.launch(ProjectFileAdapterGUI.class);
  }
}